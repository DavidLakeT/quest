/*import axios from 'axios';

const API_URL = 'http://127.0.0.1:3001/apis/citizen';

export const registerCitizen = async (citizenData) => {
    try {
        const response = await axios.post(`${API_BASE_URL}/registerCitizen`, citizenData);
        return response.data;
    } catch (error) {
        throw new Error('Error al crear ciudadano');
    }
};

export const validateCitizen = async (citizenData) => {
    try {
        const response = await axios.post(`${API_BASE_URL}/validateCitizen`, citizenData);
        return response.data;
    } catch (error) {
        throw new Error('Error al crear ciudadano');
    }
};

export const transferCitizen = async (citizenData) => {
    try {
        const response = await axios.post(`${API_BASE_URL}/transferCitizen`, citizenData);
        return response.data;
    } catch (error) {
        throw new Error('Error al crear ciudadano');
    }
};*/